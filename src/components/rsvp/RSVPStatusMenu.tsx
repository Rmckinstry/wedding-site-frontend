import { GroupData, RSVP } from "../../utility/types";

function RSVPStatusMenu({ groupData, groupRSVPs }: { groupData: GroupData; groupRSVPs: RSVP[] }) {
  return (
    <>
      <div id="rsvp-status-menu-container"></div>
    </>
  );
}

export default RSVPStatusMenu;
